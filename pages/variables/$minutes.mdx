# $minutes
Gets the current minutes number (without 0 at the start).

## Usage
```jacob copy
$minutes()
```

## Example(s)
This will log the current minutes to the console in blue:
```jacob copy
#log[$minutes();;blue]
```